# CodaVybes Mobile — Capacitor shell

This package turns the root responsive CodaVybes build into Android/iOS native shells.

## Requirements

- Node/npm
- Android Studio for Android
- macOS + Xcode for iOS

## First setup

From repository root:

```bash
npm install
npm run build
cd platforms/mobile-capacitor
npm install
```

Android:

```bash
npm run add:android
npm run sync:android
npm run android
```

iOS (macOS only):

```bash
npm run add:ios
npm run ios
```

After changing the React app:

```bash
npm run sync
```

After changing brand assets or before a release APK:

```bash
npm run sync:android
```

The Android source icon and splash live in `assets/icon.png` and
`assets/splash.png`; `sync:android` regenerates the native launcher/splash
assets and then syncs the shared web bundle.

## Android notification permission

The shell includes `@capacitor/local-notifications` for Android device alerts generated by the live CodaVybes notification bridge.

After pulling this version, run:

```bash
cd platforms/mobile-capacitor
npm install
npm run sync:android
```

Then rebuild/reinstall the APK. On Android 13+, open CodaVybes Settings and tap **Enable** under System notifications to show the native permission prompt. The app creates the high-importance `codavybes-alerts` Android channel automatically.

This native local channel fixes alerts received by the running app. Notifications while the app is fully terminated require Firebase Cloud Messaging plus a server-side push sender; do not place Firebase service-account secrets in the app.

## Google OAuth return to the app

The shared frontend uses `app.codavybes.social://auth/callback`. Capacitor's generated
Android project registers `@string/custom_url_scheme`; confirm its value is
`app.codavybes.social` in `android/app/src/main/res/values/strings.xml` after sync.

Add these URLs to Supabase Dashboard → Authentication → URL Configuration →
Redirect URLs:

- `app.codavybes.social://auth/callback`
- `app.codavybes.social://auth/reset-password`

Then run `npm install`, `npm run sync`, and rebuild/reinstall the APK.

## Production checklist

Before store submission add native implementations for:

- Remote push delivery while the app is fully terminated (APNs/FCM)
- Camera/gallery permissions for profile/media uploads
- Haptics for Aura and game feedback
- Universal/App Links for CodaVybes URLs

Never place the Supabase service-role/secret key in Capacitor config or bundled JavaScript.
